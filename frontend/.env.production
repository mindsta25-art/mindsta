# Production Environment Variables

# API Base URL (Your production API endpoint)
VITE_API_URL=https://api.yourdomain.com/api

# Paystack Public Key (Use LIVE key in production)
VITE_PAYSTACK_PUBLIC_KEY=pk_live_your_paystack_live_public_key

# JWT Secret (Same as backend)
VITE_JWT_SECRET=your-production-jwt-secret-min-32-characters-long

# Environment
VITE_NODE_ENV=production

# MongoDB URI (Used by some frontend utilities if needed)
# VITE_MONGODB_URI=mongodb+srv://...
